% Settings

% Packages

% Filters a harmless warning of glossaries/tracklang. It must be loaded before glossaries.
\usepackage{silence}
\WarningFilter{tracklang}{No}

% Style
\usepackage[spanish,english,es-tabla]{babel}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{lmodern}  % Use a modern font (that is not pixelated)
\usepackage[protrusion=true, expansion=true]{microtype}  % Use microtype to improve readability
\usepackage{amsmath,amssymb}
\usepackage{xcolor}

% Palette (defined early: used by titles, tables and figures)
\definecolor{upmblue}{RGB}{0,82,147}  % institutional-ish blue, used across the document
\definecolor{pendingorange}{RGB}{176,93,0}
\definecolor{upmgold}{RGB}{174,155,109}  % Pantone 873 C
\definecolor{umpred}{RGB}{230,25,49}  % Pantone Red 032 C
\definecolor{upmgrey}{RGB}{227,227,227}  % Pantone 877 C (Gris claro)

% Tables
\usepackage{tabularx}  % tables spanning the full text width
\usepackage{multicol}  % multi-columns in tables
\usepackage{multirow}  % multi-rows in tables
\usepackage{longtable}  % tables spanning more than one page
\usepackage{booktabs}  % nicer horizontal rules in tables
\usepackage{ltablex}
\usepackage{colortbl}  % \rowcolor for table headers

% Graphics
\usepackage{tikz}  % drawing custom shapes
\usetikzlibrary{positioning, arrows.meta, fit, backgrounds, calc}
\usepackage{pgfplots}  % data charts (bar/slope/log)
\pgfplotsset{compat=1.18}
% Shared, minimal-chartjunk style for all data charts (institutional palette).
\pgfplotsset{
  upmbase/.style={
    width=\linewidth,
    font=\small,
    axis line style={draw=black!55},
    tick style={draw=black!40},
    label style={font=\small\color{black!75}},
    tick label style={font=\footnotesize\color{black!70}},
    every axis title/.style={font=\small\bfseries\color{upmblue!85!black}, at={(0.5,1.04)}, anchor=south},
    grid style={draw=black!12},
  },
  % Horizontal bar charts (categorical y): clean, x grid only, category labels kept.
  upmhbar/.style={
    upmbase, xbar,
    axis y line=left, y axis line style={draw=none}, ytick style={draw=none}, ytick=data,
    axis x line=bottom, xmajorgrids,
    bar width=11pt, enlarge y limits=0.16, xmin=0,
    every axis plot/.append style={bar shift=0pt},
  },
}
\usepackage{listings}  % code blocks
\lstdefinestyle{upmcode}{
  basicstyle=\footnotesize\ttfamily,
  backgroundcolor=\color{upmblue!4},
  frame=single, framerule=0.4pt, rulecolor=\color{upmblue!30},
  framesep=5pt, xleftmargin=7pt, xrightmargin=5pt,
  aboveskip=1.2em, belowskip=1em,
  breaklines=true, showstringspaces=false, tabsize=2,
  literate={á}{{\'a}}1 {é}{{\'e}}1 {í}{{\'i}}1 {ó}{{\'o}}1 {ú}{{\'u}}1 {ñ}{{\~n}}1 {Á}{{\'A}}1 {É}{{\'E}}1 {Í}{{\'I}}1 {Ó}{{\'O}}1 {Ú}{{\'U}}1 {Ñ}{{\~N}}1 {ü}{{\"u}}1 {¿}{{?`}}1 {«}{{\guillemotleft}}1 {»}{{\guillemotright}}1,
}
\lstset{style=upmcode}

\usepackage{graphicx}  % manage images
\usepackage[export]{adjustbox}

% Text & layout helpers
\usepackage{setspace}
\usepackage{ragged2e}
\usepackage{comment}
\usepackage{parskip}  % paragraph spacing (suppress indentation)
\usepackage{enumitem}
\usepackage{emptypage}  % no headers in empty pages
\usepackage{csquotes}
\usepackage{fancyhdr}  % personalizar encabezado
\usepackage[small,hang,bf]{caption}  % caption options
\usepackage{subcaption}
\usepackage{titlesec}  % elegant chapter/section headings
\usepackage[bottom]{footmisc}

% Improve the elegance of the style of the references
\usepackage[square,numbers]{natbib}

% Geometry
\usepackage[a4paper]{geometry}
\newgeometry {top=2.5cm, bottom=2.5cm, right=2.5cm, left=2.5cm, marginparwidth=2cm, marginparsep=3mm, headheight=15pt}

% Hyperlinks (load before glossaries)
\usepackage{hyperref}
\hypersetup{
    plainpages=false,  % anclajes de página por \thepage...
    hypertexnames=false,
    colorlinks=true,  % Enable colored links
    linkcolor=black,  % Color for internal links
    urlcolor=blue,
    citecolor=blue
}

% Glossaries (acronyms)
\usepackage[acronym,nonumberlist,nogroupskip,nopostdot,toc,nomain,section=section]{glossaries}
\usepackage{glossary-longbooktabs}  % booktabs-styled glossary tables
\setacronymstyle{long-short}
\setlength{\glsdescwidth}{0.78\linewidth}
\addto\captionsspanish{
  \renewcommand*{\entryname}{\textcolor{upmblue!85!black}{Sigla}}%
  \renewcommand*{\descriptionname}{\textcolor{upmblue!85!black}{Significado}}
}
\makeglossaries

% Customize document
\setlength{\parskip}{10pt}
\raggedbottom  % With paragraph spacing, it does not force vertical padding on the page

% Header and footer
\fancyhf{}  % Clear the default header and footer
\renewcommand{\headrulewidth}{0.2pt}

\fancyhead[RO]{\nouppercase \leftmark}  % Define the header for odd and even pages
\fancyhead[LE]{\thesisAuthor}  % Define the header for odd and even pages
\fancyfoot[C]{\thepage}

\addto\captionsenglish{\renewcommand{\contentsname}{Table of Contents}}

% Increase indentation in lists
\newlist{mydescription}{description}{1}
\setlist[mydescription,1]{labelindent=2em, leftmargin=2em}

% Heading style: small-caps blue chapter label, bold title, blue rule
\titleformat{\chapter}[display]
  {\normalfont}
  {\color{upmblue!85!black}\scshape\Large\chaptertitlename~\thechapter}
  {1.5ex}
  {\bfseries\huge}
  [{\vspace{1.5ex}{\color{upmblue!85!black}\titlerule[1.2pt]}}]
\titlespacing*{\chapter}{0pt}{-20pt}{30pt}

\titleformat{\section}
  {\normalfont\Large\bfseries\color{upmblue!60!black}}
  {\thesection}{1em}{}
\titleformat{\subsection}
  {\normalfont\large\bfseries\color{upmblue!40!black}}
  {\thesubsection}{1em}{}

% Table & caption style
% Captions: bold blue label ("Tabla 2.1"), small text
\captionsetup{labelfont={bf,color=upmblue!85!black}}

% Slightly taller table rows
\renewcommand{\arraystretch}{1.2}

% X columns from tabularx left alligned (ragged righ)
\renewcommand{\tabularxcolumn}[1]{>{\raggedright\arraybackslash}p{#1}}

% Column header style: use in the header row of every table, e.g. \rowcolor{upmblue!8} \colhead{Etapa} & \colhead{Cometido} \\
\newcommand{\colhead}[1]{\textcolor{upmblue!85!black}{\textbf{#1}}}

% Project-specific helpers

% Requirements: own counter with format R1, R2... to reference them with \ref without the number of the chapter/table.
\newcounter{requisito}
\renewcommand{\therequisito}{R\arabic{requisito}}
% \reqid{etiqueta} prints «R1» in the cell and attaches the tag for \ref.
\newcommand{\reqid}[1]{\refstepcounter{requisito}\therequisito\label{#1}}

% Objectives: same mechanics, O1, O2... format to refernce with \ref.
\newcounter{objetivo}
\renewcommand{\theobjetivo}{O\arabic{objetivo}}
% \objid{etiqueta} prints «O1» and attaches the tag with \ref.
\newcommand{\objid}[1]{\refstepcounter{objetivo}\theobjetivo\label{#1}}

% Project name
\usepackage{xspace}
\newcommand{\proyecto}{\textsf{\textbf{\textcolor{upmblue}{UPM Match}}}\xspace}
% Placeholder marker for content that cannot be written yet (results, figures, final numbers...).
% Search for \pendiente before submitting: the document must compile to zero occurrences in the final version.
\newcommand{\pendiente}[1]{\textcolor{pendingorange}{\textbf{[Pendiente:} #1\textbf{]}}}


\usepackage[textwidth=2cm]{todonotes} 
\newcommand{\duda}[1]{%
  \textsuperscript{\textcolor{pendingorange}{$\star$}}
  \todo[
    color=green!15,      
    bordercolor=green,   
    textcolor=black,             
    size=scriptsize,             
    linecolor=green,     
    noline=false                 
  ]{#1}\xspace
}